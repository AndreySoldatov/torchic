{
    "abs": r#"output[idx] = abs(data[idx]);"#,

    "acos": r#"output[idx] = acos(data[idx]);"#,
    "asin": r#"output[idx] = asin(data[idx]);"#,
    "atan": r#"output[idx] = atan(data[idx]);"#,
    
    "sin": r#"output[idx] = sin(data[idx]);"#,
    "sinh": r#"output[idx] = sinh(data[idx]);"#,
    "cos": r#"output[idx] = cos(data[idx]);"#,
    "cosh": r#"output[idx] = cosh(data[idx]);"#,
    "tan": r#"output[idx] = tan(data[idx]);"#,
    "tanh": r#"output[idx] = tanh(data[idx]);"#,
    
    "ceil": r#"output[idx] = ceil(data[idx]);"#,
    "floor": r#"output[idx] = floor(data[idx]);"#,
    "round": r#"output[idx] = round(data[idx]);"#,
    "trunc": r#"output[idx] = trunc(data[idx]);"#,
    
    "exp": r#"output[idx] = exp(data[idx]);"#,
    "log": r#"output[idx] = log(data[idx]);"#,
    "sqrt": r#"output[idx] = sqrt(data[idx]);"#,
    "inversesqrt": r#"output[idx] = inverseSqrt(data[idx]);"#,
    
    "relu": r#"output[idx] = max(data[idx], 0.0);"#,
    "leaky_relu": r#"output[idx] = max(data[idx], 0.01 * data[idx]);"#,
    "sigmoid": r#"output[idx] = 1.0 / (1.0 + exp(-data[idx]));"#,
}